name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout source code
      uses: actions/checkout@v3
    - name: Login to Docker Hub
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKER_HUB_USERNAME }}
        password: ${{ secrets.HUB_ACCESS_TOKEN }}
    - name: Build the image and push to Docker Hub
      uses: docker/build-push-action@v2
      with:
        context: .
        push: true
#        tags: vlkuzmichev/my-django-pg-docker:latest, VlKuzmichev/django-pg-docker: ${{ github.run_number }}
#      run: docker build . --file Dockerfile --tag my-django-pg-docker:$(date +%s)
